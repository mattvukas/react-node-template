{
  "name": "react-node-template",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "concurrently \"cd src/server && nodemon\" \"cd src/client && npm start\"",
    "logs": "heroku logs -t",
    "heroku-postbuild": "cd src/client && npm && npm run build"
  },
  "dependencies": {},
  "nodemonConfig": {
    "ext": "js,json,yml,yaml"
  },
  "engines": {
    "node": ">=12.16.1",
    "npm": ">=6.13.4"
  },
  "devDependencies": {}
}
